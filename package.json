{
  "name": "user",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix front-end\"  \" cd back-end && .\\myenv\\Scripts\\activate && set FLASK_APP=.\\myenv\\app.py && set FLASK_ENV=development && set FLASK_DEBUG=1 && flask run\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
